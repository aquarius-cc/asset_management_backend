
---

### 📄 文档 2：后端子引擎 `/asset_management_backend/AGENTS.md` (v9.2.1)

# 后端 AI 执行子引擎配置 (Backend Engine)
> 版本：v9.2.1 | 最后更新：2026-07-13
> 职责：Django/DRF 代码生成、ORM 规范、状态机逻辑、后端测试门禁、代码规模与复杂度管控

## §1 核心执行协议

### 1.1 读取优先
- 执行任务前，**必须**读取 `../Rules_Fiels/backend-business-rules.md` 并逐条核对 B1-B10 及 BR-1~BR-7。
- 执行任务前，**必须**读取 `../Rules_Fiels/backend-testing-rules.md` 并逐条核对 T1-T8（含变异测试）。
- **必须**读取根级 `/AGENTS.md` 的 §6（安全）、§7（可观测性）、§8（AI鲁棒性）、§9（AI交互与写作规范），并确保生成的代码和文本输出符合所有相关条款。

### 1.2 架构分层铁律
必须严格遵循五层架构，禁止跨层调用：
1. **Model**：仅定义字段、元数据、基础 `save` 重写。
2. **Selector**：封装所有 `QuerySet` 过滤与预加载 (`select_related`)。
3. **Service**：实现所有业务逻辑与状态流转，**必须**添加 `@transaction.atomic`。
4. **Serializer**：负责数据校验与格式化，**禁止**处理数据库操作。
5. **View**：仅接收请求、调用 Service、返回 Response，使用 `@extend_schema` 生成文档。

### 1.3 硬性代码标准与质量门禁
- **格式化与类型检查**：必须通过 `ruff check .` 和 `mypy . --strict`。
- **导入顺序**：标准库 → 第三方库 → Django → 本地 apps。
- **敏感操作标注**：涉及删除、批量更新，必须在代码注释中添加 `# [HALT]`。
- **复杂度门禁**：代码完成后，必须运行 `ruff check . --select C90 --max-complexity 10`，若超标则触发 `[HALT]`。
- **测试门禁**：代码完成后，**必须**运行 `pytest --cov=. --cov-fail-under=80`，并单独检查 Service 层覆盖率 ≥ 90%。若未通过，触发 `[HALT]` 并补充测试用例。
- **变异测试**：必须运行 `mutmut run` 并确保通过率 ≥ 80%（参见 T8）。
- **复用规则索引**：编码前必须检查是否存在可复用的 Selector/Service/Utils，详细复用规范见 `../Rules_Fiels/backend-business-rules.md` 第四节（BR-1 ~ BR-3）。
- **规模规则索引**：函数行数、文件行数、调用链深度等量化红线见 `../Rules_Fiels/backend-business-rules.md` 第四节（BR-4 ~ BR-7）。

## §2 规则索引
| 规范类型 | 文件路径 |
|:---|:---|
| 业务规范 | `../Rules_Fiels/backend-business-rules.md` (B1-B10 + 状态机 + BR-1~BR-7) |
| 测试规范 | `../Rules_Fiels/backend-testing-rules.md` (T1-T8) |

## §3 最终审计票（后端专用）

### 必填项

```markdown
[审计票-后端 - 必填项]
- 读取规范：已读 AGENTS & Rules
- 分层架构：Model/Selector/Service/Serializer/View 职责清晰
- 事务装饰器：已添加 / 无需
- CT-1[√] CT-3[√] CT-5[√]
- DR-1[√] DR-5[√/豁免]
- SC-1[√] SC-3[√]
- 跨端契约：未破坏
- 红线触发：无 / [HALT]已确认
- 建议提交：是 / 否
```

### 自检项

```markdown
[审计票-后端 - 自检项]
- 测试：CT-2[√] CT-4[√] CT-6[√]
- DRY：DR-2~DR-4[√] DR-6[√]
- 安全：SC-2[√] SC-4~SC-8[√]
- 可观测性：OC-1~OC-3[√] OC-4[~] OC-5[~] OC-6[√] OC-7[~]
- AI鲁棒性：AR-1~AR-5[√]
- AI行为：Fact-1[√]（事实基线）
- 写作风格：Style-1~Style-3[√]
- 覆盖率：整体 XX% / Service 层 XX%
```
## §4 子域规则演进协议（后端局部自适应）
本协议仅管理后端专属规范。若变更涉及跨端契约，必须上报根级。

### 4.1 自主变更范围
- 修改 B1-B10 中的后端专属规范（如分页参数名、日志格式）。

- 调整状态机内部流转逻辑（如增加中间状态），前提是不改变对外 API 字段枚举值。

- 修改 Service/Selector 分层约定或 BR-1~BR-7 中的后端量化阈值。

- 调整后端测试细则 T1-T8（如新增测试目录规则、变异测试配置）。

### 4.2 变更执行步骤
1. **差异分析**：对比新需求与 backend-business-rules.md 或 backend-testing-rules.md。

2. **影响评估**：统计受影响的 models.py / services.py / tests/ 数量。

3. **生成补丁**：拟定对目标文件的修改，标记 `[PATCH-BE]`。

4. **[HALT] 审批**：展示补丁内容，等待用户确认。

5. **应用与追溯**：
    - 更新文件头部版本号（如 v1.0 → v1.1）并追加变更日志。

    - 若本次修改涉及 AGENTS.md 自身流程/规则的调整，头部版本号须 +1（如 v9.0.0 → v9.1.0），并在本文件末尾"变更日志"中记录。

### 4.3 冲突上报规则
- 涉及资产枚举键名变更（如 in_store 改 on_shelf）→ 上报根级。

- 涉及 API 响应结构变化 → 上报根级。

- 涉及数据库表结构重命名 → 触发根级红线。

## §5 变更日志
- v9.2.1 (2026-07-13)：审计票自检项同步根级v3.4.1，增加Fact-1（事实基线）和Style-1~Style-3（写作风格）检查行。

- v9.2.0 (2026-07-13)：同步根级v3.4.0，§1.1读取优先增加根级§9（AI交互与写作规范）引用。

- v9.1.0 (2026-07-07)：同步根级v3.1.0，审计票增加CT-6和OC-7，OC-5标记改为`[~]`。

- v9.0.0 (2026-07-07): 适配根级 v3.0.0，增加安全/可观测性/AI鲁棒性契约引用；审计票扩展；增加变异测试门禁（T8）。

- v8.3.0 (2026-07-07): 新增量化阈值规范（BR-4~BR-7）及对应的复杂度门禁命令（ruff C90）。

- v8.2.0 (2026-07-07): 新增测试门禁与 backend-testing-rules.md 索引；§4 追加自身版本变更追溯规则。

- v8.1.1 (2026-07-07): 初始版本，建立后端五层架构与局部自适应协议。