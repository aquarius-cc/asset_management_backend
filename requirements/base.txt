# ==================== 资产管理系统 - 生产依赖 ====================
# 核心框架依赖（统一使用 == 锁定版本，确保环境一致性）
# 【易错点】更新版本时必须同步测试，避免依赖冲突

# Django 核心框架
# 版本说明：Django 6.0.5 (LTS) - 长期支持版本
Django==6.0.5

# Django REST Framework
# 版本说明：DRF 3.16 与 Django 6.0 兼容
djangorestframework==3.16.0

# CORS 跨域支持
django-cors-headers==4.7.0

# 过滤支持
django-filter==25.1

# 数据库连接（PostgreSQL）
# psycopg3 为 Django 5.2+/6.0 推荐驱动,binary 免编译安装
psycopg[binary]==3.3.4

# 图像处理
pillow==11.3.0

# 二维码生成（HIGH-15）
qrcode[pil]==8.0

# HTTP 请求库
requests==2.32.5

# 环境变量管理
python-decouple==3.8

# JWT 认证
djangorestframework-simplejwt==5.5.1
PyJWT==2.10.1

# API 文档生成
# 【易错点】drf-spectacular 0.28+ 支持 Django 6.0
drf-spectacular==0.28.0
drf-spectacular-sidecar==2025.1.24

# WebSocket 支持（P1-8 实时通知）
channels==4.3.2

# ==================== 安装命令 ====================
# pip install -r requirements/base.txt
# 推荐使用国内镜像加速：
# pip install -r requirements/base.txt -i https://mirrors.aliyun.com/pypi/simple/
