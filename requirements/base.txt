# ==================== 资产管理系统 - 生产依赖 ====================
# 核心框架依赖（统一使用 == 锁定版本，确保环境一致性）
# 【易错点】更新版本时必须同步测试，避免依赖冲突

# Django 核心框架
# 版本说明：Django 5.2.6 (LTS) - 修复52个已知CVE
Django==5.2.17

# Django REST Framework
# 版本说明：DRF 3.16 与 Django 5.2 兼容
djangorestframework==3.16.0

# CORS 跨域支持
django-cors-headers==4.7.0

# 过滤支持
django-filter==25.1

# 数据库连接（PostgreSQL）
# psycopg3 为 Django 5.2+/6.0 推荐驱动,binary 免编译安装
psycopg[binary]==3.3.4

# 图像处理
# 版本说明：Pillow 12.3.0 修复25个已知CVE
pillow==12.3.0

# 二维码生成（HIGH-15）
qrcode[pil]==8.2

# HTTP 请求库
# 版本说明：requests 2.33.0 修复1个已知CVE
requests==2.33.0

# requests 依赖（显式 pin，避免系统级版本冲突）
# 版本说明：urllib3 2.7.0 修复4个已知CVE
urllib3==2.7.0
chardet==5.2.0

# 环境变量管理
python-decouple==3.8

# JWT 认证
# 版本说明：PyJWT 2.13.0 修复12个已知CVE（含 crit header 未验证）
djangorestframework-simplejwt==5.5.1
PyJWT==2.13.0

# API 文档生成
# 【易错点】drf-spectacular 0.29+ 支持 Django 5.2/6.0
drf-spectacular==0.29.0
drf-spectacular-sidecar==2026.4.14

# WebSocket 支持（P1-8 实时通知）
channels==4.3.2

# ASGI 服务器（替代 gunicorn 用于 WebSocket 支持）
daphne==4.2.2

# Redis 连接（channels_redis 依赖）
channels-redis==4.3.0
redis==6.2.0

# Prometheus 监控（OC-4 落地）
prometheus-client==0.26.0

# ==================== 安装命令 ====================
# pip install -r requirements/base.txt
# 推荐使用国内镜像加速：
# pip install -r requirements/base.txt -i https://mirrors.aliyun.com/pypi/simple/

# ==================== 错误追踪 ====================
sentry-sdk[django]>=2.0,<3.0
