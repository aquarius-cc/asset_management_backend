-r base.txt

# 测试框架与工具（按需添加）
pytest==9.1.1
pytest-django==4.12.0
coverage==7.15.0
factory-boy==3.3.3

# M-4 压测基线（预发环境独立执行，不进生产镜像 / CI 流程）
locust==2.32.4
httpx==0.28.1  # locust HTTP 客户端依赖


# 激活虚拟环境后，执行以下命令（批量安装所有依赖）
# pip install -r requirements/dev.txt -i https://mirrors.aliyun.com/pypi/simple/