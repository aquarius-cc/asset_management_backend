# =============================================================================
# Docker Compose 生产环境覆盖配置
# =============================================================================
# ⚠️ 此文件不能独立运行，必须与 docker-compose.yml 配合使用：
#   docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# 此文件覆盖开发配置，提供生产级安全和性能设置。
# 所有密钥必须通过 .env 文件或 CI/CD 环境变量注入。
# =============================================================================

services:
  web:
    image: asset-management-backend:${IMAGE_TAG:-latest}
    restart: always
    # 【优雅停机】docker-entrypoint.sh 以 exec "$@" 启动，Daphne 即为 PID 1，
    # 可直接接收 Docker 下发的 SIGTERM 并优雅退出（停止接新连接、等待在途请求）。
    # 默认宽限期仅 10s，不足以让长请求与活跃 WebSocket 排空，故显式延长至 45s。
    stop_grace_period: 45s
    stop_signal: SIGTERM
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.production
      - SECRET_KEY=${SECRET_KEY}
      - DEBUG=False
      - ALLOWED_HOSTS=${ALLOWED_HOSTS}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
      - SENTRY_DSN=${SENTRY_DSN:-}
      - APP_VERSION=${APP_VERSION:-unknown}
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"

  postgres:
    restart: always
    # Postgres 收到 SIGTERM 进入 smart shutdown 前的缓冲，避免被 SIGKILL 造成 WAL 恢复
    stop_grace_period: 60s
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"

  nginx:
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"

  certbot:
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  backup:
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
