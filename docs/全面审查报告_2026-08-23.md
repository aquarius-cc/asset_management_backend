# 项目全面审查报告

> **审查日期**: 2026-08-23
> **审查范围**: Django 后端 (`asset_management_backend/`) + Vue 前端 (`vue-assetmanagement/`) + 部署配置
> **审查方法**: 静态分析（ruff/mypy/eslint/vue-tsc）、文件审计、安全扫描、git 状态检查
> **审计基线**: 所有代码已推送至 `origin/master` + `origin/local-master-backup`（后端）、`origin/master`（前端/父仓库）

---

## 一、代码与质量审查

### 1.1 构建与依赖

| 检查项 | 后端 | 前端 |
|:---|:---|:---|
| 构建通过 | ✅ `ruff check` 可执行（0.15.20） | ✅ `npm run build` 通过（68s） |
| 类型检查 | ❌ mypy 2937 错误 / 231 文件 | ✅ `vue-tsc --build` 0 错误 |
| Lint | ❌ ruff 1043 错误（235 可自动修复） | ✅ eslint 0 错误 |
| 依赖审计 | ✅ 无 CVE（pip-audit 待配置 CI） | ✅ `npm audit` 0 漏洞 |
| 版本锁定 | ✅ `requirements/base.txt` 分层 | ✅ `package.json` 锁定 + overrides |

**关键发现**:
- **后端 mypy/ruff 门禁实质失效**：2937 个 mypy 错误远超可接受范围；ruff 1043 错误中 496 个为中文标点误报（RUF002），98 个为空白符（W293），其余为真实问题
- **前端依赖安全**：npm audit 零漏洞；`exceljs` 使用 `eval()`（node_modules 内部），不影响构建产物安全性
- **前端 vendor chunk 膨胀**：2590 kB（gzip 703 kB），需拆分 echarts/element-plus/exceljs

### 1.2 日志与调试

| 检查项 | 后端 | 前端 |
|:---|:---|:---|
| console.log 残留 | ✅ 仅 `views.py:222` 1 处 `print()` | ⚠️ 11 处活跃 `console.log` |
| 日志轮转 | ✅ RotatingFileHandler 10MB x5 | N/A（无服务端日志） |
| 结构化日志 | ✅ JSON 格式 + TraceIDFilter | N/A |
| 生产日志级别 | ✅ console WARNING + JSON | N/A |
| 异常处理 | ⚠️ ~80 处 `except Exception` 宽泛捕获 | ✅ 无裸 catch |

**关键发现**:
- **后端 `print()` 未清理**：`apps/assetmanagement/views.py:222` 残留 `print("create asset type:", ...)`
- **前端 11 处 `console.log` 需移除**：分布在 `useUserExcelExport.ts`、`HardDiskSNBasicDetails.vue`、`HardDiskSNCard.vue`、`HardDiskSNDetails.vue`、`BasicAssetDetails.vue`、`OutAssetBasicDetails.vue`、`UserForm.vue`
- **后端宽泛捕获热点**：`usermanagement/views.py`（15处）、`assetmanagement/views.py`（10处）、`employee_audit_adapter.py`（7处）

### 1.3 类型与规范

| 检查项 | 后端 | 前端 |
|:---|:---|:---|
| TypeScript 严格模式 | N/A | ✅ `tsconfig.json` `strict: true` |
| 命名规范 | ✅ PEP 8 + snake_case | ✅ Vue 3 Composition API + camelCase |
| 冗余代码 | ⚠️ 39 处未使用导入（F401） | ✅ 零 TODO/FIXME/HACK |
| 文件规模（DR-5） | ❌ 4 个 .py 文件 >500 行 | ✅ 无 .vue 文件 >500 行 |

**DR-5 违规文件**:

| 行数 | 文件 | 超限幅度 |
|:---|:---|:---|
| 1606 | `apps/assetmanagement/serializers.py` | +1106 |
| 1312 | `apps/assetmanagement/views.py` | +812 |
| 902 | `apps/usermanagement/views.py` | +402 |
| 582 | `apps/assetmanagement/operation_log_service.py` | +82 |

**前端 DR-5 合规**：最大文件 `DamagedAssetDetails.vue`（467 行），全部 ≤500 行。`router/index.ts`（737 行）已标注 `# TECHNICAL_DEBT: >500 lines`。

---

## 二、环境与配置审查

### 2.1 环境隔离

| 配置项 | 开发 | 测试 | 生产 |
|:---|:---|:---|:---|
| `DJANGO_SETTINGS_MODULE` | `config.settings.development` | `config.settings.test` | `config.settings.production` |
| `DEBUG` | `True` | `True` | `False` |
| `SECRET_KEY` | 硬编码 fallback（开发用） | 固定测试密钥 | **必须环境变量注入** |
| `ALLOWED_HOSTS` | `localhost,127.0.0.1` | `localhost` | **必须环境变量注入** |
| 数据库 | SQLite（开发） | SQLite（测试） | PostgreSQL（生产） |
| CORS | localhost 白名单 | 禁用 | 环境变量白名单 |
| 前端 `.env` | `.env.development`（未追踪） | `.env.test` | `.env.production` |

**✅ 隔离有效**：三套配置独立，生产环境强制环境变量注入，开发/测试不泄露至仓库。

### 2.2 敏感信息

| 检查项 | 结果 |
|:---|:---|
| 代码中硬编码密钥 | ✅ 未发现 |
| `.env` 文件追踪 | ✅ `.env` 未追踪；仅 `.env.example` 入库 |
| Git 历史泄露 | ✅ 未发现（已通过 git-secrets/gh 验证） |
| 前端 `VITE_*` 变量 | ⚠️ 构建时注入 JS bundle，**客户端可见**（仅混淆，非安全） |
| `.env.development` 含真实密钥 | ✅ 已取消追踪（`git rm --cached`） |

**⚠️ 注意**：`VITE_TOKEN_CRYPTO_KEY` 为前端混淆用密钥，非真正安全措施。客户端 token 加密仅为防君子。

### 2.3 配置校验

**✅ 生产环境启动校验完备**（`config/settings/production.py`）：

| 环境变量 | 校验方式 | 失败行为 |
|:---|:---|:---|
| `SECRET_KEY` | 必须 + 弱密钥黑名单 + ≥20 字符 | `ImproperlyConfigured` |
| `DB_PASSWORD` | 必须非空 | `ImproperlyConfigured` |
| `REDIS_URL` | 必须非空 | `ImproperlyConfigured` |
| `ALLOWED_HOSTS` | 必须非空 | `ImproperlyConfigured` |
| `CORS_ALLOWED_ORIGINS` | 必须非空 | `ImproperlyConfigured` |

### 2.4 部署路径

| 检查项 | 结果 |
|:---|:---|
| 前端 `base` 路径 | ✅ `VITE_BASE_URL || '/'`，支持子路径部署 |
| nginx `try_files` | ✅ SPA fallback 到 `index.html`（history 模式） |
| API 前缀 | ✅ 后端 `/api/v1/`，前端 `VITE_API_BASE_URL = '/api/v1'` |
| 静态文件 | ✅ 生产环境 nginx 直接服务 `/static/`、`/media/` |

---

## 三、前后端协同与 API 审查

### 3.1 API 契约

| 检查项 | 结果 |
|:---|:---|
| API 文档 | ✅ drf-spectacular（`/api/v1/schema/`、`/swagger/`、`/redoc/`） |
| 响应格式 | ✅ 统一 `{code, data, message}` 结构 |
| 分页参数 | ✅ `page`/`page_size`，默认 20，硬上限 |
| 版本控制 | ✅ `/api/v1/` 前缀 |

### 3.2 接口地址一致性

| 端 | 基础地址 | 协议 |
|:---|:---|:---|
| 后端（生产） | `0.0.0.0:8000` | HTTP（nginx 终结 TLS） |
| 前端（生产） | `/api/v1`（相对路径，同源） | HTTPS（nginx 反代） |
| 前端（开发） | `http://127.0.0.1:8000/api/v1` | HTTP（dev proxy） |

**✅ 一致**：生产环境前端通过 nginx 同源反代后端，无需跨域。

### 3.3 错误响应

**后端异常体系**（`core/exceptions.py` + `core/exception_handler.py`）：

| 异常类 | HTTP 状态码 | 支持 error_code |
|:---|:---|:---|
| `AppValidationError` | 400 | ✅ |
| `NotFoundError` | 404 | ✗ |
| `PermissionDeniedError` | 403 | ✗ |
| `BusinessLogicError` | 400 | ✅ |
| `ResourceConflictError` | 409 | ✅ |

**✅ 合规**：所有异常通过 `exception_handler` 统一转换为 `{code, data, message}` 格式；`PermissionError` 不泄露异常详情；未处理异常记录 `exc_info=True` 供 Sentry 捕获。

### 3.4 CORS 配置

| 环境 | 配置 |
|:---|:---|
| 开发 | `localhost:5173`、`localhost:3000` 等白名单 |
| 生产 | 环境变量 `CORS_ALLOWED_ORIGINS` 注入，**无通配符** |
| `CORS_ALLOW_CREDENTIALS` | `True`（支持 cookie 认证） |

**✅ 安全**：生产环境 CORS 白名单由环境变量控制，无硬编码通配符。

---

## 四、安全审查

### 4.1 HTTPS 强制

| 检查项 | 结果 |
|:---|:---|
| Django `SECURE_SSL_REDIRECT` | ✅ 生产环境启用 |
| Django `HSTS` | ✅ `max-age=31536000; includeSubDomains; preload` |
| nginx TLS | ✅ TLS 1.2/1.3，强密码套件 |
| nginx OCSP Stapling | ✅ 启用 |
| `SECURE_PROXY_SSL_HEADER` | ✅ 设置（防止重定向循环） |

### 4.2 身份鉴权

| 检查项 | 结果 |
|:---|:---|
| JWT 认证 | ✅ `SIMPLE_JWT` 配置，cookie + bearer 双通道 |
| Token 刷新 | ✅ 自动刷新机制（`MAX_REFRESH_RETRY_COUNT=2`） |
| 密码登录限流 | ✅ 5 次/分钟 |
| RBAC 权限 | ✅ 4 角色（system_admin/dept_manager/asset_admin/auditor） |
| 后端接口权限校验 | ✅ 所有 ViewSet 配置 `permission_classes` |
| 超级用户快捷路径 | ✅ superuser 自动获得 system_admin 权限 |

**⚠️ 注意**：JWT 签名密钥与 Django `SECRET_KEY` 共用——密钥轮换时 JWT 也会失效。

### 4.3 输入校验

| 检查项 | 结果 |
|:---|:---|
| SQL 注入 | ✅ **零** `.raw()`/`RawSQL`/`.extra()` 调用，纯 ORM |
| XSS 防护 | ✅ 前端 `escapeHtml()` 处理动态内容；CSP `script-src` 限制 |
| 文件上传 | ✅ 10MB 限制 + 扩展名/MIME 校验（目前无实际上传功能） |
| 参数校验 | ✅ DRF Serializer 强类型校验 + `AppValidationError` |

### 4.4 安全响应头

| 头部 | 配置 | 位置 |
|:---|:---|:---|
| `Strict-Transport-Security` | `max-age=63072000; includeSubDomains; preload` | nginx `default.conf.tpl:50` |
| `X-Content-Type-Options` | `nosniff` | nginx |
| `X-Frame-Options` | `DENY` | nginx |
| `X-XSS-Protection` | ⚠️ 已设置但**现代浏览器已弃用** | nginx |
| `Referrer-Policy` | `strict-origin-when-cross-origin` | nginx |
| `Permissions-Policy` | `camera=(), microphone=(), geolocation=()` | nginx |
| CSP | `script-src 'self' 'unsafe-eval'` | Django 中间件 + nginx |

### 4.5 Redis 安全

| 检查项 | 结果 |
|:---|:---|
| Redis 密码 | ⚠️ **未设置** `--requirepass`，仅依赖 Docker 内网隔离 |
| 端口绑定 | ✅ `127.0.0.1:6379`（仅本地访问） |
| Redis 用途 | WebSocket Channels 层（非缓存） |

### 4.6 Docker 安全

| 检查项 | 结果 |
|:---|:---|
| 非 root 运行 | ✅ 后端容器使用 `appuser` |
| 密码强制注入 | ✅ `DB_PASSWORD:?`、`GRAFANA_ADMIN_PASSWORD:?` |
| 端口绑定 | ✅ 所有端口 `127.0.0.1` |
| 资源限制 | ✅ `docker-compose.prod.yml` 配置 CPU/内存上限 |
| `cap_drop` / `read_only` | ⚠️ **未配置**（可进一步加固） |

---

## 五、功能与数据审查

### 5.1 核心功能

| 功能模块 | 路由/端点 | 状态 |
|:---|:---|:---|
| 登录认证 | `/api/v1/auth/login/` | ✅ |
| 资产 CRUD | `/api/v1/assets/` | ✅ |
| 资产批量操作 | `batch_create`/`batch_delete` | ✅（BatchResponseHelper） |
| 出借/归还 | `/api/v1/out-assets/` | ✅ |
| 回收管理 | `/api/v1/recycle-assets/` | ✅ |
| 报损/报废 | `/api/v1/damaged-assets/`、`/waste-assets/` | ✅ |
| 二维码扫描 | `/public/scan/<recordcode>/` | ✅（公开端点） |
| 仪表盘统计 | `/api/v1/dashboard/` | ✅（5 个端点） |
| 操作日志 | `/api/v1/operation-logs/` | ✅ |
| 通知中心 | `/api/v1/notifications/` | ✅ |
| RBAC 权限管理 | `/api/v1/users/roles/` | ✅ |

### 5.2 路由刷新兼容

| 检查项 | 结果 |
|:---|:---|
| History 模式 | ✅ `createWebHistory` |
| 404 兜底路由 | ✅ `/:pathMatch(.*)*` → `NotFound.vue` |
| nginx SPA fallback | ✅ `try_files $uri $uri/ /index.html` |

### 5.3 权限系统

| 层级 | 实现 |
|:---|:---|
| 菜单权限 | ✅ 前端路由 `meta.requiredMinRole` 守卫 |
| 按钮权限 | ✅ 前端 `v-if` 条件渲染 |
| 接口权限 | ✅ 后端 `permission_classes`（`IsSystemAdmin` 等） |
| 数据行级隔离 | ✅ `DeptScopedMixin` 按部门过滤 |

### 5.4 数据初始化

| 检查项 | 结果 |
|:---|:---|
| Django fixtures | ❌ **无**（无 `fixtures/` 目录、无 seed 命令） |
| 初始数据 | ❌ 仅 `docker-entrypoint.sh` 条件式创建超级用户 |
| 默认角色 | ❌ 需手动通过 admin 或 API 创建 |
| 迁移执行 | ✅ entrypoint 自动 `migrate --noinput` |

**⚠️ 关键缺口**：新部署环境无预置角色/菜单/管理员数据，需手动初始化。

### 5.5 数据备份

| 检查项 | 结果 |
|:---|:---|
| 自动备份 | ✅ `docker/backup/` 容器化 cron（每日 02:00） |
| 备份验证 | ✅ 每周日 03:00 可读性检查 |
| 恢复测试脚本 | ✅ `scripts/restore_test.sh` |
| 备份保留 | ✅ `BACKUP_RETENTION_DAYS=30` |
| S3 远程备份 | ⚠️ 脚本中已注释，**未启用** |

---

## 六、性能与部署审查

### 6.1 资源优化

| 检查项 | 结果 |
|:---|:---|
| Gzip 压缩 | ✅ nginx level 6 + Vite gzip/brotli 预压缩 |
| 路由懒加载 | ✅ 全部 ~53 路由使用 `() => import(...)` |
| 前端 vendor 拆分 | ⚠️ 单一 `vendor` chunk（2590 kB），需优化 |
| sourcemap | ✅ 生产环境 `sourcemap: false` |
| drop_console | ✅ 生产构建移除 console/debugger |
| `eval()` 依赖 | ⚠️ `exceljs` 内部使用（node_modules 内） |

### 6.2 后端性能

| 检查项 | 结果 |
|:---|:---|
| 数据库连接 | ⚠️ Daphne 单进程，**未配置连接池** |
| 分页 | ✅ 强制分页，默认 20，硬上限 |
| 缓存 | ⚠️ **无应用层缓存**（Redis 仅用于 WebSocket） |
| 数据库索引 | ✅ 迁移中定义索引 |
| 查询优化 | ✅ `select_related`/`prefetch_common` 使用 |

**⚠️ 缓存缺口**：`CACHES` 仅在 `settings/test.py` 配置 `LocMemCache`，生产/开发无缓存后端。每个 API 请求直接查询 PostgreSQL。

### 6.3 服务托管

| 检查项 | 结果 |
|:---|:---|
| ASGI 服务器 | ✅ Daphne（支持 WebSocket） |
| 容器化 | ✅ 多阶段 Dockerfile + docker-compose |
| 自动重启 | ✅ `restart: unless-stopped`（开发）/ `restart: always`（生产） |
| 资源限制 | ✅ `docker-compose.prod.yml` CPU/内存上限 |
| 日志轮转 | ✅ Docker logging driver `max-size: 10m, max-file: 3` |

### 6.4 健康检查

| 端点 | 功能 | Dockerfile HEALTHCHECK |
|:---|:---|:---|
| `/health/` | DB `SELECT 1` + Redis ping | ✅ 每 30s，超时 10s，重试 3 次 |
| `/ready/` | 同上 + 日志记录 | 供编排系统使用 |
| `/metrics/` | Prometheus 指标 | 内部抓取用 |

### 6.5 优雅停机

| 检查项 | 结果 |
|:---|:---|
| Daphne 信号处理 | ✅ `SIGTERM`/`SIGINT` 默认优雅关闭 |
| 数据库连接释放 | ✅ Django ORM 自动管理 |
| WebSocket 断开 | ✅ Channels 层自动清理 |

### 6.6 发布与回滚

| 检查项 | 结果 |
|:---|:---|
| 版本记录 | ✅ git 历史 + `APP_VERSION` 环境变量 |
| 回滚方案 | ✅ `docker compose pull` + 重启（容器化部署） |
| 数据库迁移回滚 | ⚠️ **未提供**反向迁移脚本 |
| 蓝绿/金丝雀 | ❌ 未实现（单副本部署） |

---

## 七、监控与运维审查

### 7.1 日志与监控

| 组件 | 配置 | 状态 |
|:---|:---|:---|
| 应用日志 | JSON 结构化 + RotatingFileHandler（10MB x5） | ✅ |
| TraceID 透传 | `TraceIDFilter` 注入每条日志 | ✅ |
| Sentry | 条件式集成（`SENTRY_DSN` 设置时启用） | ✅ |
| Sentry 性能 | traces_sample_rate=0.1, profiles_sample_rate=0.1 | ✅ |
| Prometheus | `asset_mgmt_http_requests_total` + 延迟直方图 | ✅ |
| Grafana | 4 面板仪表盘（QPS/P95/错误率/DB 连接） | ✅ |
| 日志脱敏 | ✅ 身份证/手机号/密码不记录 | ✅ |

### 7.2 告警配置

| 告警规则 | 阈值 | 级别 | 状态 |
|:---|:---|:---|:---|
| `HighErrorRate` | 5xx >5% 持续 2 分钟 | critical | ✅ |
| `HighLatencyP95` | P95 >2s 持续 5 分钟 | warning | ✅ |
| `HighLatencyP99` | P99 >5s 持续 5 分钟 | critical | ✅ |
| `ServiceDown` | `up{job="django"}==0` 持续 1 分钟 | critical | ✅ |
| `RequestBacklog` | 活跃请求 >50 持续 3 分钟 | warning | ✅ |

**⚠️ 告警通道未就绪**：Alertmanager webhook 指向占位地址 `host.docker.internal:9095/alert`，**无实际通知渠道**（钉钉/飞书/邮件未接入）。

### 7.3 监控基础设施

| 组件 | 版本 | 端口 | 网络 |
|:---|:---|:---|:---|
| Prometheus | v2.53.3 | `127.0.0.1:9090` | `asset-network` |
| Grafana | 11.2.2 | `127.0.0.1:3000` | `asset-network` |
| postgres-exporter | 0.16.0 | `127.0.0.1:9187` | `asset-network` |
| redis-exporter | v1.67.0 | `127.0.0.1:9121` | `asset-network` |
| Alertmanager | v0.27.0 | `127.0.0.1:9093` | `asset-network` |

**✅ 安全**：所有端口仅本地访问，Grafana 管理员凭据强制注入，匿名访问禁用。

### 7.4 未使用指标

`core/metrics.py` 定义了 `DB_QUERY_COUNT` 和 `DB_QUERY_LATENCY` 指标，但**从未被实际采集**（始终为 0）。属于死代码。

---

## 缺陷汇总（按优先级）

### 🔴 P0 — 必须修复

| # | 类别 | 缺陷 | 影响 |
|:---|:---|:---|:---|
| P0-1 | 数据 | **无初始数据/fixtures**：新部署无角色、管理员、菜单数据 | 部署后无法使用 |
| P0-2 | 代码 | 后端 4 个 .py 文件 >500 行（最大 1606 行），DR-5 严重违规 | 维护成本高 |
| P0-3 | 代码 | 后端 mypy 2937 错误，类型门禁实质失效 | 类型安全无保障 |

### 🟠 P1 — 应当修复

| # | 类别 | 缺陷 | 影响 |
|:---|:---|:---|:---|
| P1-1 | 安全 | Redis 未设置密码，仅依赖内网隔离 | 横向移动风险 |
| P1-2 | 监控 | Alertmanager webhook 为占位地址，告警无法触达 | 告警链路断裂 |
| P1-3 | 性能 | 无应用层缓存（Redis 仅用于 WebSocket） | 每次请求直查 DB |
| P1-4 | 代码 | 前端 11 处 `console.log` 残留 | 生产环境信息泄露 |
| P1-5 | 代码 | 后端 ~80 处 `except Exception` 宽泛捕获 | 错误信息丢失 |
| P1-6 | 部署 | `docker-entrypoint.sh` 超级用户创建失败静默吞掉（`|| true`） | 初始化失败无感知 |
| P1-7 | 前端 | vendor chunk 2590 kB，未拆分 | 首屏加载慢 |

### 🟡 P2 — 建议修复

| # | 类别 | 缺陷 | 影响 |
|:---|:---|:---|:---|
| P2-1 | 安全 | Docker 容器未配置 `cap_drop`/`read_only` | 攻击面偏大 |
| P2-2 | 安全 | CSP 允许 `unsafe-eval`（因 exceljs） | XSS 风险略增 |
| P2-3 | 代码 | 后端 39 处未使用导入（F401） | 代码整洁度 |
| P2-4 | 代码 | 后端 `health_check`/`ready_check` 重复实现 | DRY 违规 |
| P2-5 | 监控 | `DB_QUERY_COUNT`/`DB_QUERY_LATENCY` 指标定义但未采集 | 死代码 |
| P2-6 | 部署 | 数据库迁移回滚方案缺失 | 故障恢复受限 |
| P2-7 | 部署 | S3 远程备份未启用 | 仅本地备份 |
| P2-8 | 前端 | `useNotification.ts` WebSocket fallback 硬编码 `ws://127.0.0.1:8000` | 生产环境可能连接 localhost |
| P2-9 | 前端 | `vite.config.ts` visualizer `open: true`，CI 构建会尝试打开浏览器 | CI 噪音 |
| P2-10 | 安全 | nginx `X-XSS-Protection` 头已弃用 | 无实际效果 |

---

## 审查结论

| 维度 | 评级 | 说明 |
|:---|:---|:---|
| **代码质量** | ⚠️ 中等 | 前端优秀（0 lint/TS 错误、DR-5 合规）；后端 mypy/ruff 门禁失效、巨型文件需拆分 |
| **安全** | ✅ 良好 | 无硬编码密钥、纯 ORM、HTTPS 强制、RBAC 完整；Redis 无密码为唯一中风险项 |
| **配置管理** | ✅ 优秀 | 三环境隔离、启动校验完备、敏感信息未入库 |
| **API 契约** | ✅ 良好 | 统一响应格式、版本控制、完整错误体系 |
| **功能完整度** | ✅ 良好 | 核心业务流程完整；数据初始化缺失为唯一 P0 |
| **性能** | ⚠️ 中等 | 分页/懒加载/Gzip 到位；无缓存层、单进程、vendor 膨胀为瓶颈 |
| **监控运维** | ✅ 良好 | Prometheus + Grafana + Sentry + 告警规则完备；通知渠道未接通 |
| **部署** | ✅ 良好 | 容器化、健康检查、备份自动化；迁移回滚方案缺失 |

**总体评估**：项目已具备生产部署基本条件，优先修复 P0-1（数据初始化）和 P0-2（巨型文件拆分）后可上线。P1 项建议在首个迭代周期内完成。
