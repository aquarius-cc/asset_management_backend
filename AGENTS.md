# AGENTS.md 精简版（AI最高开发准则）

> 适用：资产管理系统后端（Django/DRF），红线绝对禁止，必做强制执行。

## 规则优先级

业务专属红线 > 通用红线 > docs细则 > 通用规范 > 技能集(.trae/) > Trae CN

## 前置总则

- 任务前必须通读；模糊立即询问（✨ **先问不猜**：绝不自行猜测 API/配置/需求）。
- 代码100%对齐现有apps风格，禁止自创。
- 资产业务安全/审计/一致性优先一切。

## 🔴 通用红线（15条）

1. **单一职责**：Model字段≤25，Service函数≤30行，View仅HTTP。
2. **最小侵入**：仅改任务范围，禁止无关重构、过度设计、无用抽象（✨ **简约至上 + 精确编辑**）。
3. **类型严格**：全标注，禁用Any、`# type: ignore`。
4. **可回退**：一键回滚后mypy/ruff/test全过，关键路径必有回退方案（✨ **一次只烧一艘船**）。
5. **安全**：输入Serializer全校验，双重权限，ORM防注入，禁硬编码密钥。
6. **文档同步**：API变更加注解和docs。
7. **绝对导入**：`from apps.xxx, core.xxx`，禁相对。
8. **模型**：继承BaseModel，含create_time/update_time/is_delete，表名蛇形，查滤is_delete=False。
9. **信号**：仅post_save/delete，判断created，全异常捕获，不影响主流程。
10. **返回格式**：统一 `{"code":200,"msg":"","data":{}}`。
11. ✨ **先问不猜**：不确定时必须向用户确认或列出选项（不存在API、错误配置、模糊需求），**禁止自行猜测**。
12. ✨ **别让模型做非语言工作**：能用确定代码判断的（if-else、正则、重试次数、路由分支），绝不由模型决策，必须硬编码。
13. ✨ **一人写码原则**：多Agent协作时，**只指定一个Agent负责写代码**，其余只读或审查。
14. ✨ **契约驱动**：编码前先定义清晰接口契约（OpenAPI/GraphQL/Protobuf），前后端严格按契约实现，避免集成爆炸。
15. ✨ **原子提交**：强制每次Git提交**只包含一个逻辑变更**（单一职责），禁混杂无关改动。

## 🔴 业务专属红线（8条）

1. **审计**：资产核心数据变更100%记录快照+操作人。
2. **数据隔离**：rewrite get_queryset按组织过滤。
3. **事务**：影响库存/状态操作全包裹事务。
4. **状态流转**：严格按预设，前置校验，禁跨状态。
5. **数据不可逆**：已审核单据禁修改，需红冲/反向。
6. **唯一标识**：资产编码全局唯一，不可改，事务内生成。
7. **敏感保护**：采购价等字段级权限+脱敏。
8. **防越权**：审批/批量操作双重校验，对象级 check_object_permissions。

## 🟡 开发流程（6步不可跳）

1. **需求分析**  
   → ✨ **先扫目录**：执行 `ls`/`tree` 扫描项目结构，建立全局观。  
   → ✨ **无提示上下文加载**：自动探测项目环境（读取 `requirements.txt`、`.env.example`、`README.md`），无需用户手动粘贴。  
   → 读全文档，明边界、审计、存量影响。

2. **方案设计**  
   → ✨ **目标驱动**：只给验收标准，不一步步教AI怎么做。  
   → ✨ **一次只烧一艘船**：必须设计回退方案，关键路径失败能安全回归。  
   → ✨ **契约驱动**：涉及后端接口变更时，先定义OpenAPI片段，双方确认。  
   → 最小变更、数据兼容。

3. **编码**  
   → ✨ **别让模型做非语言工作**：所有业务分支、重试逻辑用代码确定（if-else、正则），不交给模型推理。  
   → 单向依赖 Model→Signals→Serializer→Service/Selector→View；事务；审计日志；幂等；N+1优化。

4. **测试**  
   → ✨ **强制校验**：必须提供可执行的自动化验证标准（测试用例、脚本），证明“完成”，禁止口头宣称。  
   → 单元/集成/权限/并发；禁Mock核心ORM；命名 `test_should_…_when_…`。

5. **自检**  
   → mypy/ruff/test全通过，手验API、权限、兼容。  
   → ✨ **原子提交检查**：本次变更是否只含一个逻辑改动？若是，方可提交。  
   → ✨ **一人写码确认**：若多Agent协作，确保唯一写代码者。

6. **文档**  
   → 同步drf-spectacular、字典、流程图、PR说明回滚方案。

## 🟡 环境与命令

- Python 3.12+, Django 6.0.5, DRF 3.15+, MySQL 8.0。
- 依赖分层requirements/，禁跨层增依赖。
- **关键命令**：`makemigrations`（可逆+存量兼容）、`mypy .`（零错误）、`ruff check .`（零告警）、`test`（核心覆盖100%）、`check --deploy`（上线前必过）。

## 🟡 测试铁律

- 覆盖率≥85%，资管核心≥100%。
- 仅Mock外部依赖，绝对禁Mock ORM/事务/核心业务。
- 必须覆盖：单元（Service/Selector全函数）、集成全流程、越权、事务回滚、并发。
- ✨ **强制校验**：每次声称完成前，必须运行完整测试套件并通过，不得依赖用户手动验证。

## 🟡 前后端分离规范

- JWT认证，Token过期+刷新，敏感操作二次验证。
- RESTful，版本URL控制，统一成功/错误结构。
- 全局异常处理，不暴露堆栈；分页参数 `page`/`page_size`；写操作幂等号。
- 跨域显式配置，禁止`*`；drf-spectacular文档同步（✨ **契约驱动落地**）。

## 🔵 核心文件要点

- **models**：继承BaseModel，外键显式，索引高频字段，asset_code唯一。
- **serializers**：ModelSerializer，区分增/改/查，全量校验，单据前置校验。
- **services(写) / selectors(读)**：单函数≤30行，全标注，写加@transaction.atomic，同步审计日志，查询防N+1。
- **views**：ModelViewSet优先，仅调Service/Selector，重写get_queryset隔离，check_object_permissions，分页，注解。
- **signals**：仅post_save/delete，try-except，仅审计/非核心同步。

## ✅ 提交自检点

- 红线0违反，无注入/硬编码/越权，绝对导入，无物理删除，类型全标注，审计全留痕。
- mypy/ruff/test全过，迁移可逆，事务包裹，无N+1，幂等无脏数据，敏感脱敏。
- 状态合规，核心逻辑在Service，已审核不可改，数据一致，回滚方案就绪。
- ✨ 原子提交：本次提交只包含一个逻辑变更，审查时清晰可追溯。
- ✨ 多Agent场景：已明确唯一写代码的Agent，其余仅做读/审查。
- ✨ 强制校验：测试脚本已运行并全通过，不依赖“看起来没问题”。
- ✨ 先问不猜：整个开发过程中无自行猜测的API或配置，所有不确定处已向用户确认。
